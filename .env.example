# Copy to .env.local and fill in as needed.
# Only variables prefixed with VITE_ are exposed to the client bundle.

# Base URL for a future order/checkout API (unused while the app is client-only).
# VITE_API_URL=
